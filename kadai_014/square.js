// 教材通りの記述
//与えられた引数num乗算し、その値を出力する関数を定義する
const addnum = (num) => {
  console.log(num*10);
}
// 関数を呼び出し、引数として数値を渡す
addnum(10);

// 検索でヒットした記述
const square = (num) => {
    const result = num ** 2;
    console.log(result);
}
//100が出力される  
  square(10);    